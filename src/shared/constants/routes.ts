export const APP_NAME = "Dog Dex";

export const LOCALES = ["en", "vi"] as const;

export const ROUTES = {
  home: "/",
  dex: "/dex",
  scan: "/scan",
  profile: "/profile",
  login: "/login",
  register: "/register",
} as const;
